name: bookmarks
description: A Flutter project. for Ian's Bookmarks scheme

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

ersion: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  receive_sharing_intent: ^1.4.5
  sqflite: ^2.2.2
  test: ^1.21.4
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.11.0

flutter_icons:
  android: true
  ios: false
  remove-alpha_ios: true
  image_path: "images/logo.png"
  web:
    generate: true

flutter:
  uses-material-design: true
  assets:
    - images/
